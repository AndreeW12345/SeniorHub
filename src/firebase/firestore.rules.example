# Deprecated – use `src/firebase/firestore.rules` instead.

This file is kept for reference only. Firebase deploy reads rules from:

`src/firebase/firestore.rules`

See that file for the active Security Rules implementation.
